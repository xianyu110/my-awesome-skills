你好！我是你的**爆款视频拆解助手**。根据你上传的视频内容，这是一个典型的**硬核技术教程类**视频，专注于通过实操演示来讲解 AI Agent 工具（Claude Code & Codex）的高级用法。

以下是详细的脚本拆解分析：
* * *

# Part 1：完整脚本

**【黄金前3秒：核心钩子】** **00:00** 每个 Skill 只占约 100 Tokens，来看一下 Agent Skills 是怎么运行的。

**【背景铺垫与环境配置】** **00:03** 因为 Agent Skills 是 Claude Code 这边率先定义并且发布的，我们先在 Claude Code 里面配置一下 skills，视频的后半段我们再来看其他的 agent 工具。 **00:14** 最近 Claude Code 安装方式发生了变化，我们先来快速的过一遍。如果已经安装好了 Claude Code 可以直接跳到屏幕上的这个时间点。 **00:22** 这里我们来到 Claude Code 官网，复制一下这个安装命令。然后来到桌面，右键在终端打开，把安装命令粘贴进来执行，这样就安装完成。 **00:31** 如果你有 Claude 官网账户的话，直接敲命令 claude 登录官网账户直接使用就可以了。如果没有官网账户，我们也可以把一些其他的模型接入 Claude Code。 **00:40** 这里我们来到 C盘用户你的用户名 .claude 这个文件夹（苹果电脑的话是屏幕上这个地址）。然后我们新建一个文件，文件的名字叫做 settings.json。 **00:50** 接下来把这段粘贴进来，这里我用的模型是智谱的 GLM，所以这里的 Auth Token 我是在智谱的 AI 开放平台创建了一个，粘贴进来，保存一下。 **01:00** 我们来到用户文件夹，找到这个 .claude.json 文件，把这一句添加上，这样就可以跳过 Claude Code 的登录。

**【核心概念讲解：文件结构】** **01:07** 在 Claude Code 中，skills 是以文件夹的形式存放在项目目录 .claude/skills 里面的。每个 skill 就是一个文件夹，每个 skill 都应该至少包含一个 SKILL.md 的定义文件。 **01:19** 定义文件里面存放的是 skill 的元数据层与指令层的内容。在 skill 的这个文件夹里面，还可以放入其他类型的辅助文件，这些文件就是 skill 的资源层。

**【实操演示 Case 1：字幕转笔记】** **01:30** 接下来我们就按照这个文件结构，把一个 skill 先创建出来。接下来我们新建一个工程文件夹，这里我叫做 skill-project。在这里面再新建一个文件夹 .claude，在里面再新建一个文件夹叫做 skills。 **01:43** 然后再打开 skills 文件夹，在这里面就可以以文件夹的形式来定义 skills 了。我们新建一个文件夹叫做“字幕转markdown”，这是我们要定义的第一个 skill。 **01:53** 进来以后我们再新建一个 skill 的定义文件，这里必须是大写的 SKILL，扩展名是小写的 md。这里我用 vscode 打开项目文件夹，这样我们看的更清楚一些。 **02:04** 我们先来写这个 skill 的 metadata 也就是元数据。元数据用六个横杠包裹起来，里面只需要填上这个 skill 的名字，然后它的描述就可以了。 **02:13** 描述部分，最重要是写清楚 AI 应该在什么时机来调用这个 skill。 **02:19** 元数据写完以后我们来写指令。指令部分就可以写的详细一点了，这里用 markdown 格式，把指令中的重点都标记出来。你是一个专业的字幕文本处理助手，任务就是把 SRT 字幕文件转换成 markdown 笔记…… **02:35** 然后我需要它在关键的位置插入一个截图的占位符。比如原始数据是这个样的，最后插入的截图方式应该是这个样。 **02:42** 一个完整的 Skill 就定义完成了。这里的重点主要有三个：首先是目录结构，第二个是 Skill 定义文件的文件名，第三个就是这里的元数据。只要这三点符合一个 skill 的标准，那它就可以生效了。 **02:55** 我们来测试一下。我们来到项目的根目录，右键在终端打开，输入命令 claude 来启动 Claude Code。然后我们输入命令 /skills 就可以看到我们自定义的 skill。 **03:05** 这里我找了一个测试文件来测试一下（Obsidian 视频字幕）。我们直接把这个字幕文件拖拽进来，回车开始。Claude Code 意识到这是一个 SRT 的字幕文件，然后他询问是否使用字幕转 markdown 的这个 skill 进行处理。这里我们选择 yes。 **03:22** （解释运行过程）在一开始，AI 的上下文里面只有这部分元数据信息。当我们确认了使用这个 skill 以后，AI 才把下半部分的指令信息加载进上下文。 **03:33** 然后它要求读取 srt 文件，我们确认。这样就完成了转换，我们的 skill 就运行成功了。在项目的根目录就多了一个 markdown 格式的笔记文件。

**【实操演示 Case 2：创意生成】** **03:47** 我们再来看一个例子。我们可以在 skills 这个文件夹下面再新建一个文件夹。我的第二个 skill 的名字叫做“来点选题”。在下面创建一个 SKILL.md 文件，填写好元数据还有指令。 **03:58** 这个 skill 的调用时机是当用户视频创作选题枯竭的时候，为用户提供对应的选题思路。（输入指令：我是视频作者，参考我的数据提供灵感...） **04:12** 这里重启一下 Claude Code。我说我不知道做什么视频好了。Claude Code 要调用“来点选题” skill。它调用这个 Skill 为我生成了一些选题灵感，还是挺有意思的。

**【底层逻辑复盘】** **04:22** 现在我们有了两个 skills，我们来总结一下 AI 的调用过程。在这个例子里面，用户先把提问发送到 Agent 也就是 Claude Code。 **04:30** Claude Code 会扫描本地所有的 SKILL.md 文件，提取它们的元数据，形成一个可用的技能列表，连同用户的提问一起发送给 AI 大模型。 **04:40** 由于元数据都比较简短，所以这一步占用的模型上下文是非常少的。接下来 AI 根据自己的知识进行判断。AI 可以选择不使用 skill，直接给出回答；也可以选择其中一个 skill 来解决问题。 **04:53** 然后 Claude Code 来执行 skill 的加载，把 skill 的指令细节（SKILL.md 下半部分）发送给 AI。这里体现了 skill 提示词按需加载、渐进式披露的设计模式。最后 AI 综合以上所有信息生成一个完整的回答。

**【进阶技巧：全局配置 & 跨平台迁移】** **05:09** 这里再补充一下，我们创建的这两个 skills，除了能在这个项目里面生效，也可以把它变成一个全局的 skills，对所有的项目都生效。 **05:18** 我们来到用户目录下的 .claude 配置文件夹，这里我们可以新建一个 skills 目录，就可以把之前创建好的 skills 直接粘贴到配置目录里面。这样它们就变成了一个全局的 skill。

**【跨工具应用：Codex】** **05:35** 我们编写好的 skills，除了能在 Claude Code 里面使用，也可以在其他的 AI Agent 工具里面使用，比如 Codex。 **05:47** Codex 的安装步骤没有什么变化。因为 Agent Skills 在 Codex 里面还是一个实验性的功能，这里需要改一点配置。我们来到 .codex 文件夹，打开 config.toml 配置文件，添加上这一段，把 skills 的功能开启起来。 **06:04** 把 skills 从 Claude Code 迁移到 Codex 非常简单，只需要把路径里面的 .claude 改成 .codex 就行了。 **06:11** 我们为 Codex 也新建一个工程文件夹 skill-codex。新建一个 .codex 的配置文件夹，然后我们可以直接把 Claude Code 这边的技能包复制一下到 Codex 里面。 **06:27** 我们来到 Codex 项目的根目录，启动起来。输入命令 /skills 就可以查看。我给 Codex 说我不知道做什么视频，Codex 也给我生成了 15 个选题。

**【资源扩展与结尾】** **06:41** 除了我们自己手写 skills，在 Github 上面也有很多社区里分享的非常成熟好用的 skills。比如这个开源项目 Awesome Claude Skills。 **06:55** 我们先把代码下载下来，比如我们就用这个“域名头脑风暴”。我把整个文件夹拖拽到 Codex skills 文件夹里面，然后重启一下 Codex。 **07:05** （演示域名生成功能）效果不错。所以使用一个别人写好的 skill 包非常的简单，只需要把整个文件夹拖拽进来就可以了。 **07:21** 接下来我们来看 Agent Skills 的进阶用法，也就是 skills 的资源层。我们打开一个 skill 的文件夹，这里面不仅能存放这个 SKILL.md 文件……（视频结束）
* * *

# Part 2：结构拆解

## 1\. 框架结构
*   **黄金钩子**：用“极低Token消耗”作为利益点引入，快速吸引技术型观众。
*   **环境搭建**：快速过一遍工具安装与环境配置（防劝退，同时也服务新手）。
*   **核心演示**：通过“字幕转笔记”和“选题生成”两个具体案例，手把手演示 Skill 的创建流程。
*   **原理复盘**：图解 AI Agent 的运行机制（元数据 -> 决策 -> 加载），提升内容深度。
*   **横向扩展**：讲解全局配置、跨工具迁移（Codex）以及利用开源社区资源，极大地拉高了内容的实用价值。

## 2\. 为什么有效
*   **解决了具体痛点**：将抽象的 "Agent" 概念具象化为“自动化处理文件”和“提供灵感”的工具。
*   **保姆级实操**：从文件夹创建到代码粘贴，细节极度丰富，观众跟着做一定能成功。
*   **价值感延伸**：不仅教怎么用 Claude，还教怎么迁移到 Codex，甚至怎么用现成的 GitHub 资源，干货密度极高。

## 3\. 心理触发点
*   **情绪**：**掌控感**。通过简单的配置就能让 AI 自动处理复杂任务，给观众带来极强的技术掌控满足感。
*   **动机**：**效率优化**。利用“只占100 Tokens”和“自动化流程”击中技术人员追求极致效率和低成本的心理。
*   **认知偏差**：**可得性偏差**。演示过程非常流畅简单（拖拽、复制粘贴），让观众觉得“我也能轻松做到”。

## 4\. 复刻所需背景
*   **软件环境**：Windows 11 (视频演示环境)，VS Code，PowerShell/终端。
*   **必备工具**：Claude Code 或 Codex (开源替代品)，智谱 AI API (或其他大模型 API)。
*   **素材准备**：SRT 字幕文件样例，GitHub 上 "Awesome Claude Skills" 的项目链接。

## 5\. 万能模板

**(适用于技术工具/插件教程类视频)**

1.  **钩子**：这个工具/功能极度轻量（省资源/快），但能实现 \[X\] 效果。

2.  **前置**：快速演示安装/环境配置（小白友好）。

3.  **核心概念**：用一句话+画面解释它的运行逻辑（如：文件夹就是插件）。

4.  **实战 Case A**：演示一个**功能型**用法（如格式转换），展示代码细节。

5.  **实战 Case B**：演示一个**创意型**用法（如生成内容），展示灵活度。

6.  **原理图解**：用流程图解释为什么它比传统方法好（如：按需加载 Token）。

7.  **高阶扩展**：如何全局通用？如何跨平台使用？

8.  **拿来主义**：介绍哪里可以下载现成的资源包（GitHub Repo），降低上手门槛。
* * *

**下一步建议：** 如果您想制作类似的视频，我可以为您生成一套**具体的 SKILL.md 模板代码**，或者为您规划一个更适合大众入门的简单 Case（例如：自动写周报 Agent）。需要吗？